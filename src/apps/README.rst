Apps Directory
==============

This directory contains all end-user applications.

Apps
----
- **desktop_pro**: Professional desktop application (Qt6-based) for Windows/Mac/Linux
- **ios_native**: Native iOS application using C++ core with UIKit/SwiftUI frontend
- **android_native**: Native Android application using C++ core with JNI bridge

Each app has its own README with specific build instructions.
